{
  "name": "kargo-{{plugin_name}}",
  "version": "0.1.0",
  "description": "{{plugin_description}}",
  "main": "index.js",
  "author": "{{author_name}} <{{author_email}}>",
  "license": "MIT",
  "scripts": {
    "test": "node index.js",
    "build:wasm": "javy compile index.js -o {{plugin_name}}.wasm"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "devDependencies": {
    "@javy/cli": "^0.1.0"
  }
}