# `src/plugins/host/functions.rs`

- **Path**: /Volumes/samsung_t9/kargo/kargo-cli
- **Project**: kargo-cli
- **File Hash**: d2b282a0  
- **Timestamp**: 2025-10-10T02:12:22.809983+00:00  
- **Lines of Code**: 152

---## Orphaned Methods


### `register_spawn_kargo_task()`

- **Location**: `/Volumes/samsung_t9/kargo/kargo-cli/src/plugins/host/functions.rs` (line 145)
- **Visibility**: private
- **Issue**: Function is defined but never called anywhere in the codebase

```rust

/// Register the spawn_kargo_task host function
fn register_spawn_kargo_task(host_fn_tx: mpsc::Sender<HostFunctionRequest>) -> Function {
    HostFunction::new(
        "spawn_kargo_task",
```

### Action Required:

- Evaluate the intended purpose of the orphaned method, assuming it is intended to be used by default.
- If it should be used, update this section with instructions on how to incorporate it into the codebase.
- If it is deprecated, ask for permission to remove it.
- Update this section with your findings and instructions on how to proceed.


### `register_read_file()`

- **Location**: `/Volumes/samsung_t9/kargo/kargo-cli/src/plugins/host/functions.rs` (line 51)
- **Visibility**: private
- **Issue**: Function is defined but never called anywhere in the codebase

```rust
    )
}/// Register the read_file host function
fn register_read_file(host_fn_tx: mpsc::Sender<HostFunctionRequest>) -> Function {
    HostFunction::new(
        "read_file",
```

### Action Required:

- Evaluate the intended purpose of the orphaned method, assuming it is intended to be used by default.
- If it should be used, update this section with instructions on how to incorporate it into the codebase.
- If it is deprecated, ask for permission to remove it.
- Update this section with your findings and instructions on how to proceed.


### `register_log_message()`

- **Location**: `/Volumes/samsung_t9/kargo/kargo-cli/src/plugins/host/functions.rs` (line 19)
- **Visibility**: private
- **Issue**: Function is defined but never called anywhere in the codebase

```rust
    ]
}/// Register the log_message host function
fn register_log_message(host_fn_tx: mpsc::Sender<HostFunctionRequest>) -> Function {
    HostFunction::new(
        "log_message",
```

### Action Required:

- Evaluate the intended purpose of the orphaned method, assuming it is intended to be used by default.
- If it should be used, update this section with instructions on how to incorporate it into the codebase.
- If it is deprecated, ask for permission to remove it.
- Update this section with your findings and instructions on how to proceed.


### `register_write_file()`

- **Location**: `/Volumes/samsung_t9/kargo/kargo-cli/src/plugins/host/functions.rs` (line 96)
- **Visibility**: private
- **Issue**: Function is defined but never called anywhere in the codebase

```rust
    )
}/// Register the write_file host function
fn register_write_file(host_fn_tx: mpsc::Sender<HostFunctionRequest>) -> Function {
    HostFunction::new(
        "write_file",
```

### Action Required:

- Evaluate the intended purpose of the orphaned method, assuming it is intended to be used by default.
- If it should be used, update this section with instructions on how to incorporate it into the codebase.
- If it is deprecated, ask for permission to remove it.
- Update this section with your findings and instructions on how to proceed.

---

*Generated by kargo-turd 0.1.0*

/Volumes/samsung_t9/kargo/kargo-cli